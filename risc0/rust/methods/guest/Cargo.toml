[package]
name = "replay_step"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
# If you want to try (experimental) std support, add `features = [ "std" ]` to risc0-zkvm
risc0-zkvm = { version = "3.0.5", default-features = true }


[build-dependencies]
cc = "1.0"
